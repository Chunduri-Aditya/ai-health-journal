# Core dependencies for Journal Agent (minimal install)
# Run: pip install -r requirements-core.txt
# For full features (Pinecone embeddings, Whisper): pip install -r requirements-optional.txt

# Flask web framework
Flask>=3.1.3,<4.0.0
Werkzeug>=3.1.6,<4.0.0
Jinja2==3.1.6
MarkupSafe==3.0.2
itsdangerous==2.2.0
click>=8.2.1,<9.0.0
blinker==1.9.0

# HTTP client
requests>=2.32.5,<3.0.0
urllib3>=2.7.0,<3.0.0
certifi==2025.7.14
charset-normalizer==3.4.2
idna>=3.10,<4.0.0

# Configuration
python-dotenv>=1.1.1,<2.0.0

# Data validation
pydantic==2.11.7
pydantic-settings>=2.6.0,<3.0.0
pydantic_core==2.33.2
annotated-types==0.7.0
typing_extensions==4.14.1

# Local RAG vector store (Chroma)
# In core so local retrieval works on a fresh clone. Without it the vector
# store silently falls back to a no-op even when VECTOR_BACKEND=chroma, so the
# journal history is never actually indexed. Pulls onnxruntime (the default
# on-device embedder) as a transitive dep.
# NOTE: chromadb advisories may lack a fixed release — waive in CI after review.
chromadb>=0.4.0

# Utilities
tqdm==4.67.1
sniffio==1.3.1
typing-inspection==0.4.1
distro==1.9.0

# HTTP/async (transitive deps)
anyio==4.9.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
jiter==0.10.0
